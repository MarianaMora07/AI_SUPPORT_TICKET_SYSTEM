# n8n Cloud

1. Nodo **Webhook** → método **POST**
2. Workflow **Active** (interruptor verde)
3. Copiar **Production URL** → `.env.local`
4. Reiniciar `npm run dev`
5. Admin → `/admin/users` → **Probar webhooks**

Variables: `N8N_WEBHOOK_TICKET_CREATED`, `N8N_WEBHOOK_HIGH_PRIORITY`
